{
  "1": {
    "class_type": "BananaVideo",
    "inputs": {
      "prompt": "{{prompt}}",
      "negative_prompt": "{{negative_prompt}}",
      "duration": "{{duration}}",
      "aspect_ratio": "{{aspect_ratio}}",
      "seed": "{{seed}}",
      "model_path": "{{banana_model_path}}",
      "cfg_scale": "{{cfg}}",
      "fps": "{{fps}}"
    }
  },
  "2": {
    "class_type": "SaveVideo",
    "inputs": {
      "filename_prefix": "banana_video",
      "video": ["1", 0]
    }
  }
}
