{
  "1": {
    "class_type": "LumaNanoVideo",
    "inputs": {
      "prompt": "{{prompt}}",
      "negative_prompt": "{{negative_prompt}}",
      "duration": "{{duration}}",
      "aspect_ratio": "{{aspect_ratio}}",
      "seed": "{{seed}}",
      "model_path": "{{nano_model_path}}",
      "num_frames": "{{num_frames}}",
      "guidance_scale": "{{cfg}}"
    }
  },
  "2": {
    "class_type": "SaveVideo",
    "inputs": {
      "filename_prefix": "luma_nano",
      "video": ["1", 0]
    }
  }
}
