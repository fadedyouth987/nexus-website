{
  "1": {
    "class_type": "KlingVideo",
    "inputs": {
      "prompt": "{{prompt}}",
      "negative_prompt": "{{negative_prompt}}",
      "duration": "{{duration}}",
      "aspect_ratio": "{{aspect_ratio}}",
      "seed": "{{seed}}",
      "model_path": "{{kling_model_path}}",
      "cfg_scale": "{{cfg}}",
      "motion_strength": "{{motion_strength}}"
    }
  },
  "2": {
    "class_type": "SaveVideo",
    "inputs": {
      "filename_prefix": "kling_video",
      "video": ["1", 0]
    }
  }
}
