# Project Boundaries

Jobryn is not Campaign/Launchpad and is not Nexus.

These projects may share general engineering ideas, but their code, database tables, UI modules, deployment projects, and repositories remain separate unless a specific migration is deliberately approved.

A search for `campaign`, `launchpad`, `nexus`, or `vantory` in Jobryn production source should normally return only documentation that explains this boundary.
